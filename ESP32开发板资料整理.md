# ESP32 物联网开发套件资料整理

> **适用范围。** 本文依据用户提供的《ESP32 物联网开发套件使用说明》和《ESP32 开发板原理图 1.1》整理，用于本项目的接线、固件编写、下载与故障排查。GPIO 映射以原理图网络为准；不同板卡批次与实际外设电压应以板上丝印、模组标记及实测结果为最终依据。[1] [2]

| 项目 | 内容 |
|---|---|
| 主控平台 | ESP32-WROOM-32E 系列 |
| USB/下载芯片 | CH340C |
| USB 供电 | Type-C VBUS，5 V |
| 板载逻辑电压 | AMS1117-3.3 V 输出的 3.3 V |
| 文档用途 | 板卡资源索引、引脚表、开发约束与调试清单 |

## 1. 硬件概览

该开发套件将 ESP32 最小系统、Type-C/CH340C 下载调试、自动下载、LCD/触摸、MicroSD/TF 卡座，以及 DHT11、HC-SR04、SR602/PIR、WS2812 和 NTC 等传感器接口整合在一块开发板上。使用说明列出了 UART、SPI、SDIO、I²C、LEDC/PWM、I²S、ADC、DAC、触摸与 GPIO 等可用外设能力。[1]

| 板载单元 | 主要信息 | 开发意义 |
|---|---|---|
| Type-C 接口 | 16 Pin；提供 5 V，并连接 CH340C 的 USB 数据线。[1] [2] | 一条可传输数据的 Type-C 线即可供电、下载和查看日志。 |
| CH340C | USB 转 UART0，连接 ESP32 TXD0/RXD0；DTR#/RTS# 用于自动下载电路。[2] | 正常情况下无需手动切换下载模式。 |
| ESP32 模组 | ESP32-WROOM-32E 系列，板上多个 GPIO 已固定连接到外设。[1] [2] | 新增外设前需先检查 GPIO 冲突。 |
| 1.69 英寸 LCD | 240 × 280；使用说明标注 ST7789、SPI 接口并带触摸功能。[1] | 适合显示与 LVGL 实验。 |
| MicroSD/TF 卡 | 采用 4 线 SDIO，使用 GPIO2/4/12/13/14/15。[1] [2] | 可用于文件、图片、数据与日志存储。 |
| LED/按键 | LED 接 GPIO27；用户按键接 GPIO39/VN。[1] [2] | 适合作为最小化的下载与 GPIO 验证。 |

### 资料差异

原理图的最小系统标注为 **ESP32-WROOM-32E-N8R2**，而使用说明写为 **ESP32-WROOM-32E N4R2**，并说明其配置为 4 MB Flash、2 MB PSRAM。[1] [2] 因此，涉及分区表、OTA 或大容量资源文件时，必须先读取实物模组丝印或在开发环境中检测 Flash 参数；**不应仅凭任一份资料预设 Flash 容量**。

## 2. 供电、串口、下载与按键

Type-C 的 VBUS 进入 +5 V 电源网络，AMS1117-3.3 V 生成 +3.3 V 系统电源。原理图在稳压器输入/输出侧设置 10 µF 与 0.1 µF 去耦电容。[2] 因此，ESP32 侧的信号逻辑和传感器接口应按 **3.3 V** 对待。请使用支持数据传输的 Type-C 线，而非仅充电线。

CH340C 通过 UART0 与 ESP32 通讯，DTR#/RTS# 经两只 SS8050 三极管控制 EN 与 GPIO0，从而实现自动下载。[2] 使用说明指出，如自动下载失败，可按住 BOOT 后短按复位键进入下载模式。[1]

| 控制对象 | 原理图连接 | 操作逻辑 |
|---|---|---|
| RESET（SW1） | EN 对地按键；EN 有 10 kΩ 上拉和 1 µF 电容。[2] | 轻按复位 ESP32。 |
| BOOT（SW2） | GPIO0 对地按键；GPIO0 有 10 kΩ 上拉。[2] | 下载异常时按住，再短按 RESET。 |
| 用户按键（SW3） | GPIO39/VN 对地按键；有 10 kΩ 上拉。[2] | 松开为高，按下为低；GPIO39 仅可作输入。 |
| 板载 LED（LED2） | GPIO27 经 1 kΩ 限流接 LED。[2] | **输出高电平点亮**。 |
| 串口日志 | UART0（TXD0/RXD0）经 CH340C 到 Type-C。[2] | 用于查看启动信息、异常和应用日志。 |

## 3. 板载资源与 GPIO 映射

下表仅列出原理图中明确占用或引出的板载资源，并非 ESP32 的全量 GPIO 表。若要复用某一 GPIO，必须确认当前程序没有启用相应板载外设，并评估其是否属于启动配置管脚。[2]

| 功能区 | 信号/外设 | ESP32 GPIO | 说明 |
|---|---|---:|---|
| 状态指示 | LED2 | GPIO27 | 高电平点亮，经 1 kΩ 限流。[2] |
| 用户输入 | SW3 | GPIO39（VN） | 上拉输入，按下接地；输入专用。[2] |
| DHT11 接口 | DHS | GPIO25 | 接口为信号、3.3 V、GND。[2] |
| HC-SR04 接口 | Trig | GPIO32 | 接口包括 +5 V、Trig、Echo、GND。[2] |
| HC-SR04 接口 | Echo | GPIO33 | Echo 经 100 Ω 串联进入 GPIO33。[2] |
| WS2812 接口 | RGB | GPIO26 | 单线 RGB 数据；与 LCD 背光复用。[1] [2] |
| SR602/PIR 接口 | RES | GPIO34 | 输入专用，适合人体存在/红外传感器信号。[1] [2] |
| NTC 接口 | NTC | GPIO36（VP） | 经 100 Ω 串联，适合 ADC 分压采样。[2] |
| LCD 背光控制 | BL_IO | GPIO26 | 经 JP7 与 SS8050 背光开关网络连接。[2] |
| LCD 显示 | DC_S | GPIO17 | SPI 数据/命令选择。[2] |
| LCD 显示 | CS_S | GPIO5 | SPI 片选。[2] |
| LCD 显示 | SCK_S | GPIO18 | SPI 时钟。[2] |
| LCD 显示 | MOSI_S | GPIO19 | SPI 主机输出数据。[2] |
| LCD 显示 | RST_S | GPIO21 | 显示复位。[2] |
| LCD 触摸 | SCL_T | GPIO22 | I²C 时钟，板上有 3.3 kΩ 上拉。[2] |
| LCD 触摸 | SDA_T | GPIO23 | I²C 数据，板上有 3.3 kΩ 上拉。[2] |
| LCD 触摸 | INT_T | GPIO35 | 触摸中断输入；输入专用。[2] |
| TF/SDIO | DATA2 | GPIO12 | 启动配置相关，见第 5 节。[1] [2] |
| TF/SDIO | DATA3 | GPIO13 | SDIO 数据线。[2] |
| TF/SDIO | CMD | GPIO15 | SDIO 命令线。[2] |
| TF/SDIO | CLK | GPIO14 | SDIO 时钟线。[2] |
| TF/SDIO | DATA0 | GPIO2 | 启动配置相关，见第 5 节。[1] [2] |
| TF/SDIO | DATA1 | GPIO4 | SDIO 数据线。[2] |

## 4. LCD、触摸、背光与 WS2812

LCD 为 240 × 280 的 SPI 屏，说明书标注其使用 ST7789 驱动并带触摸。[1] 显示总线使用 GPIO17、GPIO5、GPIO18、GPIO19、GPIO21；触摸总线使用 GPIO22 与 GPIO23，触摸中断使用 GPIO35。[2] 触摸 I²C 的 SCL_T、SDA_T 已各自经 3.3 kΩ 上拉至 3.3 V，程序和额外扩展应避免再次并联过小的上拉电阻。[2]

> **重要复用约束。** 使用说明明确指出，LCD 与 WS2812 接口不能同时使用；进行 WS2812 实验时需移除背光跳线帽。[1] 原理图同样显示 GPIO26 同时存在于 WS2812 的 RGB 数据网络和 LCD 背光跳线/驱动网络中。[2]

| 使用场景 | 资源状态 | 建议 |
|---|---|---|
| LCD 显示或 LVGL | 保持背光跳线帽接通。 | 不要同时向 GPIO26 发送 WS2812 时序。 |
| WS2812 灯环 | 移除 LCD 背光跳线帽。 | 使用 GPIO26 发送灯珠数据；LCD 背光将不可用。 |
| 触摸功能 | 保留 GPIO22、GPIO23、GPIO35。 | 将 GPIO35 仅配置为输入。 |

## 5. MicroSD/TF 卡与启动约束

TF 卡采用 4 线 SDIO 接法：DATA2、DATA3、CMD、CLK、DATA0、DATA1 分别对应 GPIO12、GPIO13、GPIO15、GPIO14、GPIO2、GPIO4。[2] 使用说明特别强调 GPIO12 与 GPIO2 的启动约束：GPIO12 影响 ESP32 对 Flash 工作电压的选择；GPIO2 若被上拉，可能导致程序无法烧写。因此该板没有按常见 TF 卡参考设计为这两线配置上拉电阻。[1]

| 信号 | GPIO | 设计限制 | 开发建议 |
|---|---:|---|---|
| DATA2 | GPIO12 | 影响启动时 Flash 电压选择；本板为 3.3 V Flash，资料说明未配置上拉。[1] | 不要让外部设备在上电或复位期间强拉高该脚。 |
| DATA0 | GPIO2 | 上拉可能妨碍程序烧写；资料说明未配置上拉。[1] | 下载期间避免被外设强拉高；在正常启动后再初始化 TF 卡。 |
| DATA3/CMD/CLK/DATA1 | GPIO13/15/14/4 | SDIO 信号经串联电阻连接卡座。[2] | 使用 TF 卡时不要复用为其他强驱动外设。 |

## 6. 配套传感器接口

使用说明列出 SR602 热释电人体存在传感器、12 颗 WS2812 灯环、HC-SR04 超声波模块、DHT11 温湿度模块和 10 kΩ NTC 热敏电阻。[1] 这些接口已在原理图中预接至固定 GPIO，首次测试可直接按下表配置。

| 接口 | GPIO 与供电 | 资料说明 | 首测方式 |
|---|---|---|---|
| DHT11 | GPIO25；3.3 V、GND。[2] | 单总线温湿度；资料称一次通信约 4 ms。[1] | 读取温湿度并输出串口。 |
| HC-SR04 | Trig=GPIO32，Echo=GPIO33；接口含 +5 V。[2] | 非接触测距；资料列出 2–400 cm、最高约 3 mm 精度。[1] | 读取 Echo 脉宽并换算距离。 |
| SR602/PIR | GPIO34；3.3 V、GND。[2] | 有人高电平、无人低电平。[1] | GPIO34 配置输入，打印状态变化。 |
| WS2812 灯环 | GPIO26；VCC、GND。[2] | 单线控制 12 颗 RGB 灯珠。[1] | 移除背光跳线后，以低亮度单色验证。 |
| NTC | GPIO36/VP；3.3 V、GND。[2] | 25 ℃ 时约 10 kΩ；资料列出 −20 ℃ 至 125 ℃ 范围。[1] | 先读取 ADC 原始值，再进行温度换算。 |

### HC-SR04 电平检查

原理图显示 HC-SR04 接口为 **+5 V 供电**，Echo 到 GPIO33 的路径只明确绘出 100 Ω 串联电阻，未绘出分压或专用电平转换器。[2] 常见 HC-SR04 模块的 Echo 可能接近供电电压。为保护 ESP32 的 3.3 V GPIO，接入非原配模块前应先实测 Echo 高电平；若为 5 V，应增加分压或电平转换。此项为基于原理图给出的保守电气检查，不能据此推断所有套件批次的实际信号电平。

## 7. 开发与排错顺序

建议按“串口通讯 → 板载 LED → 用户按键 → 单一传感器 → LCD/触摸 → TF 卡”的顺序验证。这样可以将 USB 驱动、下载、GPIO、协议和供电问题分别定位。第一版测试程序不应同时初始化 LCD、WS2812、TF 卡和多个传感器，以免 GPIO 复用和启动约束掩盖问题。

| 现象 | 优先检查项 | 处理方向 |
|---|---|---|
| 未出现串口或无法下载 | Type-C 数据线、CH340C 串口、USB 供电。 | 换用数据线，确认系统已识别 USB 串口。 |
| 自动下载失败 | GPIO0/EN 是否被外设占用，串口是否被其他程序占用。 | 按住 BOOT，再短按 RESET 后烧录。[1] |
| 无法正常启动 | GPIO0、GPIO2、GPIO12 的上电电平。 | 先断开 TF 卡与新增外设，恢复默认启动状态。 |
| LCD 黑屏 | 背光跳线、GPIO26 复用、SPI 初始化参数。 | 若做过 WS2812 实验，先恢复背光跳线。 |
| WS2812 与 LCD 同时异常 | GPIO26/背光资源冲突。 | 移除背光跳线后单独测试 WS2812。 |
| PIR 或按键读值异常 | GPIO34、GPIO35、GPIO39 为输入专用。 | 确认代码未将这些脚配置为输出。 |
| HC-SR04 读数异常 | Echo 电平、供电、触发周期和模块摆放。 | 先确认 Echo 不高于 3.3 V，再处理时序与滤波。 |

## 8. 编程前快速检查清单

- 板载 LED 使用 **GPIO27**，输出高电平点亮。
- 用户按键使用 **GPIO39**，输入低电平表示按下。
- DHT11、SR602/PIR、NTC 分别预留 GPIO25、GPIO34、GPIO36。
- LCD/触摸需保留 GPIO5、17、18、19、21、22、23、26、35。
- WS2812 需 GPIO26；使用时按说明移除背光跳线帽。
- TF 卡需 GPIO2、4、12、13、14、15；避免破坏 GPIO2/GPIO12 的上电状态。
- 新接 HC-SR04 前，确认 Echo 进入 GPIO33 前的高电平不超过 3.3 V。

## 参考资料

[1] 《ESP32 物联网开发套件使用说明》，用户提供 PDF，涵盖板载资源、传感器、下载方法与 TF 卡注意事项。  
[2] 《ESP32 开发板原理图 1.1》，用户提供 PDF，涵盖供电、自动下载、最小系统、传感器、SD 卡与 LCD 接口电气连接。

[1]: <https://files.manuscdn.com/user_upload_by_module/session_file/310519663889707187/WhQEUzMukJrxEkFX.pdf> "ESP32 物联网开发套件使用说明"
[2]: <https://files.manuscdn.com/user_upload_by_module/session_file/310519663889707187/xUDWDLsecVaXCxqz.pdf> "ESP32 开发板原理图 1.1"
