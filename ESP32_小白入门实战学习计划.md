# ESP32 物联网开发板 · 小白零基础实战学习计划

> **学习原则**：由浅入深、单点突破、步步有反馈。每完成一个关卡，在打卡框中勾选 `[x]`，遇到任何疑问或报错随时呼叫 AI 协助排查。

---

## 📌 开发板硬件速查表（引脚与资源）

在编写代码时，需要用到的引脚对照如下：

| 功能模块 | 引脚 / 资源 | 模式 / 特性 | 硬件与使用注意事项 |
| :--- | :--- | :--- | :--- |
| **可编程 LED2 (蓝)** | **GPIO27** | 输出 (Output) | 输出高电平 `1` 点亮，低电平 `0` 熄灭 |
| **用户按键 SW3** | **GPIO39 (VN)** | 输入 (Input Only) | 仅作输入；松开为高电平 `1`，按下为低电平 `0` |
| **ST7789 屏幕** | **GPIO5(CS), 17(DC), 18(SCLK), 19(MOSI), 21(RST)** | SPI 总线 | 1.69 英寸彩屏，分辨率 240 × 280 |
| **屏幕背光 (BL)** | **GPIO26** | 输出 (PWM/开关) | 经 JP7 跳线帽驱动背光；**与 WS2812 复用** |
| **CST816S 触摸** | **GPIO22(SCL), 23(SDA), 35(INT)** | I2C 总线 | 板载 3.3kΩ 上拉电阻；GPIO35 仅作输入 |
| **DHT11 温湿度** | **GPIO25** | 双向单总线 (JP1) | 板载 10kΩ 上拉电阻 |
| **HC-SR04 超声波** | **GPIO32(Trig), GPIO33(Echo)** | 脉冲测量 (JP2) | 原板按 3.3V 供电，测距范围 2~400cm |
| **SR602 人体红外** | **GPIO34** | 输入 (Input Only, JP5) | 仅作输入；有人输出高电平 `1`，无人输出 `0` |
| **NTC 温度采样** | **GPIO36 (VP)** | ADC 模拟输入 (JP4) | 仅作输入；板载 10kΩ/1% 精密分压电阻 |
| **WS2812 RGB 灯** | **GPIO26** | 单线 RMT 脉冲 (JP3) | **做此实验时须先拔掉 LCD 背光跳线帽** |
| **MicroSD / TF 卡** | **GPIO2, 4, 12, 13, 14, 15** | 4-bit SDIO | GPIO2/0 关联，注意不要外接强下拉 |

---

## 🚀 实战学习路线（5 阶段 · 10 关卡）

```
[阶段一: 见光验证] 第 01 关 (ESP32 串口通信) ───► 第 02 关 (ESP32 GPIO/PWM 呼吸灯)
                                                                 │
[阶段二: 输入感知] 第 04 关 (ESP32 ADC/传感器) ◄─── 第 03 关 (ESP32 按键/红外感应)
        │
[阶段三: 视觉交互] 第 05 关 (ESP32 彩屏绘图)   ───► 第 06 关 (ESP32 LVGL/电容触摸)
                                                                 │
[阶段四: 物联网]   第 08 关 (ESP32 天气 API)   ◄─── 第 07 关 (ESP32 WiFi/NTP时钟)
        │
[阶段五: 综合大作] 第 09 关 (ESP32 TF卡相册)   ───► 第 10 关 (ESP32 桌面智能气象站)
```

---

### 【阶段一：见光验证 —— 跑通环境与基础输入输出】

#### 第 01 关：ESP32 串口通信与 Hello World 打印
- [x] **打卡目标**：验证电脑与 ESP32 的下载与通信链路正常。
- **涉及引脚**：Type-C (UART0 `GPIO1/3`)
- **实验内容**：
  1. 编写最简主函数，使用 `ESP_LOGI()` 每隔 1 秒在串口打印一行 `Hello ESP32!`。
  2. 烧录固件并在 VS Code 终端打开串口监视器（Monitor）查看日志。
- **核心知识**：FreeRTOS 延时 `vTaskDelay()`、ESP-IDF 日志输出宏 `ESP_LOGI`、芯片参数读取。

#### 第 02 关：ESP32 GPIO 数字输出与 PWM 呼吸灯
- [x] **打卡目标**：学会单片机最基础的 GPIO 数字输出与 PWM 硬件定时器。
- **涉及引脚**：**GPIO27** (LED2 蓝色)
- **实验内容**：
  1. 初始化 GPIO27 为输出模式（Output）。
  2. 循环让 GPIO27 输出高电平（点亮）延时 500ms，再输出低电平（熄灭）延时 500ms（Blink 闪烁）。
  3. 使用 LEDC (PWM) 外设控制 LED2 实现 13 位分辨率的平滑“呼吸灯”渐变渐暗效果。
- **核心知识**：`gpio_config()`、`gpio_set_level()`、数字高低电平原理、LEDC PWM 占空比。

---

### 【阶段二：输入感知 —— 感受现实世界】

#### 第 03 关：ESP32 按键输入检测与人体红外感应
- [x] **打卡目标**：学会读取外部数字信号与按键状态。
- **涉及引脚**：**GPIO39** (SW3 用户按键)、**GPIO34** (SR602 红外)、**GPIO27** (LED2)
- **实验内容**：
  1. 将 GPIO39 配置为输入，读取按键状态；按下按键时翻转 LED2 的亮灭状态（实现按键开关灯）。
  2. 加入按键消抖逻辑（延时 20ms 双重确认，避免机械抖动误触发）。
  3. 读取 SR602 人体红外探头（GPIO34），有人经过时自动亮灯，离开后延时熄灭。
- **核心知识**：`gpio_get_level()`、机械按键抖动原理、软件消抖法、输入专用脚限制。

#### 第 04 关：ESP32 模拟量采集(ADC)、DHT11 温湿度与超声波测距
- [ ] **打卡目标**：学会采集模拟电压、测量时间脉冲与单总线传感器。
- **涉及引脚**：
  - NTC 测温：**GPIO36 (ADC1_CH0)**
  - HC-SR04 超声波：**GPIO32** (Trig 发射), **GPIO33** (Echo 接收)
  - DHT11 温湿度：**GPIO25**
- **实验内容**：
  1. **NTC 测温**：使用 ADC 读取 GPIO36 的电压值，结合 10kΩ 分压电阻换算出环境温度（℃）。
  2. **超声波测距**：GPIO32 发送 10µs 高电平脉冲，测量 GPIO33 返回的高电平持续时间，换算为实际距离（cm）。
  3. **DHT11 温湿度**：读取单总线数据包，校验并打印当前温度和湿度。
- **核心知识**：ADC 模数转换、参考电压校准、微妙级时间测量 `esp_timer_get_time()`。

---

### 【阶段三：视觉交互 —— 点亮彩屏与触摸】

#### 第 05 关：ESP32 驱动 1.69寸 ST7789 彩屏与几何图形渲染
- [ ] **打卡目标**：掌握 SPI 显示屏的初始化与基础几何绘制。
- **涉及引脚**：**GPIO5, 17, 18, 19, 21** (ST7789 SPI) + **GPIO26** (背光 BL)
- **实验内容**：
  1. 初始化 SPI 总线和 ST7789 驱动芯片。
  2. 点亮屏幕背光，执行清屏（刷红、绿、蓝纯色背景）。
  3. 绘制基本图形：矩形、线条、圆形，并在屏幕中心打印英文字符串。
- **核心知识**：SPI 主机模式通信、RGB565 颜色格式、屏幕显存与刷新机制。

#### 第 06 关：ESP32 搭载 LVGL v9 现代图形界面与 CST816S 电容触摸
- [ ] **打卡目标**：跑通现代嵌入式 GUI 框架，实现手指触摸交互。
- **涉及引脚**：屏幕 SPI 引脚 + **GPIO22(SCL), 23(SDA), 35(INT)** (CST816S 触摸)
- **实验内容**：
  1. 移植并运行 **LVGL v9** 图形库，启用 2MB PSRAM 存放图形缓冲区。
  2. 初始化 CST816S 触摸驱动并注册为 LVGL 输入设备。
  3. 创建 1 个美观的卡片界面：包含 1 个可点击的按钮（Button）、1 个圆弧仪表盘（Arc）和 1 个折线图（Chart）。
  4. 手指点击按钮时，LED2 亮起，同时屏幕上数值加 1。
- **核心知识**：LVGL 控件体系（Widgets）、事件回调（Events）、样式与主题（Styles）、PSRAM 动态分配。

---

### 【阶段四：无线互联 —— 融入物联网世界】

#### 第 07 关：ESP32 Wi-Fi 联网与 SNTP 毫秒级网络授时时钟
- [ ] **打卡目标**：让 ESP32 连上路由器，实现毫秒级精准网络时钟。
- **涉及模块**：ESP32 Wi-Fi + SNTP 网络时间协议
- **实验内容**：
  1. 配置 ESP32 Wi-Fi Station 模式，连接手机热点或家中 2.4G Wi-Fi。
  2. 获取 IP 地址后，向阿里云 NTP 服务器（`ntp.aliyun.com`）请求北京时间。
  3. 将精准的年、月、日、时、分、秒格式化并在屏幕或串口上实时走字更新。
- **核心知识**：Wi-Fi 事件循环机制（Event Loop）、SNTP 协议、C 语言 `time_t` 与 `tm` 结构体。

#### 第 08 关：ESP32 HTTP 客户端与实时天气 API 解析(cJSON)
- [ ] **打卡目标**：学会单片机网络请求与 API 数据解析。
- **涉及模块**：ESP-HTTP-Client + cJSON 库
- **实验内容**：
  1. 使用 HTTP GET 请求免费天气 API（如高德/心知天气 API）。
  2. 获取返回的 JSON 格式天气数据（城市、天气状况、温度、湿度、风向）。
  3. 使用 `cJSON` 库解析出温度与天气图标，动态渲染到 LVGL 屏幕上。
- **核心知识**：HTTP GET 请求头构建、JSON 字符串解析与键值提取、内存释放防泄漏。

---

### 【阶段五：进阶与综合大项目 —— 毕业设计】

#### 第 09 关：ESP32 挂载 MicroSD/TF 卡(SDIO)与 FATFS 电子相册
- [ ] **打卡目标**：利用板载 TF 卡槽读取外部文件资源。
- **涉及引脚**：**GPIO2, 4, 12, 13, 14, 15** (4-bit SDIO)
- **实验内容**：
  1. 格式化 TF 卡为 FAT32 格式，放入 240×280 的图片文件。
  2. 在 ESP32 中通过 SDMMC 驱动挂载 FATFS 文件系统。
  3. 读取 TF 卡中的图片文件，解码并全屏显示在 ST7789 屏幕上（制作电子相册）。
- **核心知识**：SDIO 4线高速传输、FATFS 虚拟文件系统、文件流读写 `fopen / fread`。

#### 第 10 关：ESP32 综合实战 —— 桌面多功能智能气象站与中控台
- [ ] **打卡目标**：融合前面所有关卡知识，完成一个功能完整的独立硬件作品！
- **完整功能矩阵**：
  1. **主页面**：联网自动对时（数字大时钟 + 星期日历），显示当地实时天气预报与室外温度。
  2. **传感器页面**：滑动切换到第二页，实时显示板载 DHT11 测得的室内温湿度 + NTC 测量值，绘制 24 小时温度变化曲线（Chart）。
  3. **智能交互**：
     - 触摸点击屏幕可以控制板载 LED2 或切换界面风格。
     - 启用 SR602 人体红外：无人时 30 秒后自动调暗/关闭屏幕背光；有人靠近时瞬间唤醒亮屏。
  4. **扩展记录**：每隔 10 分钟将当前温湿度数据存入 TF 卡中的 `sensor_log.csv` 表格中。

---

## 🛠️ 小白调试避坑黄金法则

1. **串口日志是你的第一双眼睛**：
   - 遇到任何异常或重启，终端里会输出红色的 `Guru Meditation Error` 或堆栈崩溃信息。
2. **硬件复用陷阱**：
   - **GPIO26** 默认接 LCD 背光，做 WS2812 RGB 灯环实验时，**必须取下 JP7 背光跳线帽**。
3. **只读引脚不可输出**：
   - `GPIO34`、`GPIO35`、`GPIO36`、`GPIO39` 为纯输入管脚，切勿在代码中配置为 `GPIO_MODE_OUTPUT`。
4. **内存管理好习惯**：
   - 复杂图形或大数组优先分配在 **2MB PSRAM**（使用 `heap_caps_malloc(size, MALLOC_CAP_SPIRAM)`），保留宝贵的内部 SRAM 给 Wi-Fi 和协议栈。
