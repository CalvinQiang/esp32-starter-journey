# ESP32 物联网开发套件资料整理（复核修订版）

> **适用范围。** 本文逐项复核用户提供的《ESP32 物联网开发套件使用说明》与《ESP32 开发板原理图 1.1》，用于本项目进行接线、固件编写、下载和故障排查。本文优先采用原理图的网络连接；涉及元件是否贴装、外接模块型号或板卡批次时，以实物丝印、阻值测量和上电测试为最终依据。[1] [2]

| 项目 | 内容 |
|---|---|
| 主控平台 | ESP32-WROOM-32E 系列 |
| USB/下载芯片 | CH340C |
| 主输入电源 | Type-C VBUS，+5 V |
| 逻辑电压 | AMS1117-3.3 V 产生 +3.3 V |
| 修订重点 | 更正 HC-SR04 供电、区分两颗 LED、补齐传感器电阻网络、补充 GPIO0/GPIO2 与 TF 卡硬件约束 |

## 1. 本次复核发现与修订摘要

原整理文档的主体 GPIO 映射、LCD/WS2812 复用、触摸接口及 TF 卡信号线均与原理图一致。但逐区放大复核后，发现有若干需要更正或补充的硬件细节。下表说明修订结果，其中标为“资料冲突”的事项不可在未看实物的情况下作单一结论。

| 项目 | 复核结论 | 修订后的使用原则 |
|---|---|---|
| HC-SR04 供电 | JP2 的 1 脚接 **+3.3 V**，不是 +5 V；Trig=GPIO32、Echo=GPIO33，均串 100 Ω。[2] | 使用原板接口/原配模块时按 3.3 V 供电；更换外部模块时仍应实测 Echo 电平。 |
| LED | LED1 是 +3.3 V 经 1 kΩ 到 GND 的**电源指示灯**；LED2 才是 GPIO27 经 1 kΩ 驱动的**程序可控 LED**。[2] | 程序只能控制 LED2；LED1 只反映 3.3 V 电源存在。 |
| DHT11 | 数据线 GPIO25 经 100 Ω，且由 R44=10 kΩ 上拉到 +3.3 V。[2] | 接口已具备上拉；外接时不要再并联过小的上拉电阻。 |
| NTC | GPIO36/VP 读取的是由 R9=10 kΩ/1% 上拉和外部 NTC 对地形成的分压，信号还串有 R8=100 Ω。[2] | 需要 ADC 校准与分压/热敏模型换算，不能把 ADC 原始值直接当温度。 |
| WS2812 供电 | 信号为 GPIO26，经 100 Ω；VCC 由 +5 V 经过 D1（1N4007）提供。[2] | GPIO26 与 LCD 背光复用；使用不同灯板或长线时应核验数据电平和供电。 |
| GPIO0 与 GPIO2 | R43 在原理图中为 0 Ω，连接 GPIO2 与 GPIO0。[2] | GPIO2 不只受 TF 卡影响，也与 BOOT/下载状态相关；外接设计必须避免在复位/下载时施加强拉电平。 |
| TF 卡上拉 | 说明书称 GPIO2 未上拉，但原理图显示 HS_DATA0/GPIO2 有 R19=10 kΩ 上拉；两份资料存在冲突。[1] [2] | 以实物 R19/R43 的贴装、阻值和实际烧录测试为准，不能简单假定 GPIO2 一定无上拉。 |
| TF 卡检测 | 卡座 Pin 2 标为 CD/DATA3，但接入 HS_DATA3/GPIO13；原理图未绘出独立卡检测 GPIO。[2] | 不要将 GPIO35 或其他脚误当作 TF 卡检测脚。 |

## 2. 硬件概览与资料差异

该板集成 ESP32 最小系统、Type-C/CH340C 下载调试、自动下载、1.69 英寸 LCD 与触摸、MicroSD/TF 卡，以及 DHT11、HC-SR04、SR602/PIR、WS2812 和 NTC 接口。使用说明列出 UART、SPI、SDIO、I²C、LEDC/PWM、I²S、ADC、DAC、触摸与 GPIO 等可用能力。[1]

| 板载单元 | 已核对信息 | 开发意义 |
|---|---|---|
| Type-C 与 CH340C | Type-C 为 16 Pin；VBUS 提供 +5 V。CH340C 连接 USB 数据线和 ESP32 UART0（TXD0/RXD0），DTR#/RTS# 参与自动下载。[1] [2] | 一条支持数据传输的 Type-C 线即可供电、下载并查看串口日志。 |
| ESP32 模组 | 原理图标注 **ESP32-WROOM-32E-N8R2**；使用说明标注 **ESP32-WROOM-32E N4R2**，并写为 4 MB Flash、2 MB PSRAM。[1] [2] | Flash 标称出现冲突；分区、OTA 与资源大小应以实物模组丝印和开发环境检测为准。 |
| LCD/触摸 | 说明书标注 240 × 280、ST7789、SPI 接口，并带触摸功能。[1] | 适用于显示与 LVGL 实验，但会占用多路 GPIO。 |
| TF 卡 | 使用 4 线 SDIO，连接 GPIO2/4/12/13/14/15。[1] [2] | 便于存储文件和日志，但与启动脚、上拉电阻和 R43 零欧连接有关。 |
| 两颗 LED | LED1 为电源指示；LED2 为 GPIO27 程序可控指示灯。[2] | LED2 适合最小程序验证，LED1 可用于判断 3.3 V 是否建立。 |

## 3. 供电、串口、复位与下载

Type-C VBUS 进入 +5 V 电源网络，AMS1117-3.3 V 输出 +3.3 V。稳压器输入、输出两侧均配置 10 µF 与 0.1 µF 去耦；ESP32 模组旁还有 C5=22 µF 与 C6=0.1 µF 本地去耦。[2] 原理图未给出 AMS1117、Type-C 线材、USB 口或扩展接口的可持续供电电流，**不要从图中推断可带载电流上限**。对电机、大灯带、继电器等大负载，应使用独立、共地且容量足够的电源。

CH340C 通过 UART0 与 ESP32 通讯。DTR# 与 RTS# 经两只 SS8050 控制 EN 与 GPIO0，实现自动下载。[2] 说明书指出自动下载异常时，可按住 BOOT 后短按复位进入下载模式。[1]

| 控制对象 | 原理图连接 | 使用逻辑 |
|---|---|---|
| RESET（SW1） | EN 对地按键；EN 有 R1=10 kΩ 上拉和 C7=1 µF。[2] | 轻按复位。 |
| BOOT（SW2） | GPIO0 对地按键；有 R40=10 kΩ 上拉。[2] | 下载失败时按住 BOOT，再短按 RESET。 |
| 用户按键（SW3） | GPIO39/VN 对地按键；有 R42=10 kΩ 上拉。[2] | 松开为高、按下为低；GPIO39 为输入专用。 |
| 电源指示 LED1 | +3.3 V → R25=1 kΩ → LED1 → GND。[2] | 不能由程序控制。 |
| 程序 LED2 | GPIO27 → R26=1 kΩ → LED2 → GND。[2] | 输出高电平点亮。 |
| 串口日志 | ESP32 UART0 的 TXD0/RXD0 连接 CH340C 与 Type-C。[2] | 用于查看启动信息、下载日志和异常重启原因。 |

## 4. 板载 GPIO 资源映射

下表只列出已由板载器件明确占用的 GPIO，并非 ESP32 的完整可用脚表。使用未列脚位前，也应评估其是否为启动相关脚、是否被焊盘/模组功能占用，以及与当前固件外设的冲突。[2]

| 功能区 | 信号/外设 | ESP32 GPIO | 电路/功能说明 |
|---|---|---:|---|
| 程序指示 | LED2 | GPIO27 | 高电平点亮，经 1 kΩ 限流。[2] |
| 用户输入 | SW3 | GPIO39（VN） | 上拉输入，按下接地；输入专用。[2] |
| DHT11 | DHS | GPIO25 | 信号串 100 Ω，R44=10 kΩ 上拉至 +3.3 V。[2] |
| HC-SR04 | Trig | GPIO32 | 信号串 100 Ω。[2] |
| HC-SR04 | Echo | GPIO33 | 信号串 100 Ω。[2] |
| WS2812 | RGB | GPIO26 | 数据线串 100 Ω；与 LCD 背光复用。[1] [2] |
| SR602/PIR | RES | GPIO34 | 信号串 100 Ω；输入专用。[1] [2] |
| NTC | NTC | GPIO36（VP） | R9=10 kΩ/1% 上拉形成分压，R8=100 Ω 串至 ADC。[2] |
| LCD 背光 | BL_IO | GPIO26 | 通过 JP7 与 Q3（SS8050）背光开关网络相关。[2] |
| LCD 显示 | DC_S | GPIO17 | SPI 数据/命令选择，串 100 Ω。[2] |
| LCD 显示 | CS_S | GPIO5 | SPI 片选，串 100 Ω。[2] |
| LCD 显示 | SCK_S | GPIO18 | SPI 时钟，串 100 Ω。[2] |
| LCD 显示 | MOSI_S | GPIO19 | SPI 主机输出，串 100 Ω。[2] |
| LCD 显示 | RST_S | GPIO21 | 显示复位，串 100 Ω。[2] |
| LCD 触摸 | SCL_T | GPIO22 | I²C 时钟，R36=3.3 kΩ 上拉。[2] |
| LCD 触摸 | SDA_T | GPIO23 | I²C 数据，R37=3.3 kΩ 上拉。[2] |
| LCD 触摸 | INT_T | GPIO35 | 触摸中断，串 100 Ω；输入专用。[2] |
| TF/SDIO | DATA2 | GPIO12 | R11=100 Ω；R41 标为 10 kΩ/NC，见第 6 节。[2] |
| TF/SDIO | DATA3 | GPIO13 | R12=100 Ω，R17=10 kΩ 上拉。[2] |
| TF/SDIO | CMD | GPIO15 | R13=100 Ω，R18=10 kΩ 上拉。[2] |
| TF/SDIO | CLK | GPIO14 | R14=100 Ω。[2] |
| TF/SDIO | DATA0 | GPIO2 | R15=100 Ω，R19=10 kΩ 上拉；并经 R43 与 GPIO0 关联。[2] |
| TF/SDIO | DATA1 | GPIO4 | R16=100 Ω，R20=10 kΩ 上拉；GPIO4 另有 R39=10 kΩ 上拉。[2] |

## 5. LCD、触摸、背光与 WS2812 复用

LCD 使用 GPIO17、GPIO5、GPIO18、GPIO19、GPIO21 作为显示控制/数据线，使用 GPIO22、GPIO23、GPIO35 作为触摸 I²C 与中断线。[2] SCL_T/SDA_T 已具备 3.3 kΩ 上拉，因此外接同一 I²C 总线时应避免重复并联过小上拉电阻。

> **关键复用约束。** 使用说明明确说明 LCD 与 WS2812 接口不能同时使用；做 WS2812 实验时应移除 LCD 背光跳线帽。[1] 原理图显示 GPIO26 既到 WS2812 数据线，又经 JP7、BL_IO 和 Q3 关联 LCD 背光。[2]

| 使用场景 | 跳线与资源状态 | 建议 |
|---|---|---|
| LCD 显示或 LVGL | 保持背光跳线帽接通。 | 不要在 GPIO26 上发送 WS2812 时序。 |
| WS2812 灯环 | 移除 LCD 背光跳线帽。 | 使用 GPIO26 输出数据，LCD 背光将不可用。 |
| WS2812 外接扩展 | 原板接口 VCC 来自 +5 V 经 1N4007。 | 原说明书称 3.3 V 驱动电平经实测可用；更换灯板、增加线长或改变供电时，先确认数据高电平裕量与电源去耦。 |
| 触摸功能 | 保留 GPIO22、GPIO23、GPIO35。 | GPIO35 只能配置为输入。 |

## 6. TF 卡、启动脚与零欧连接

TF 卡座的 DATA2、CD/DATA3、CMD、CLK、DATA0、DATA1 分别接 GPIO12、GPIO13、GPIO15、GPIO14、GPIO2、GPIO4；每条信号线上均有 100 Ω 串联电阻。[2] 卡座 Pin 2 虽标为 CD/DATA3，但在此原理图中连接 HS_DATA3/GPIO13，不存在独立卡检测网线。[2]

最值得注意的是 R43：原理图以 **0 Ω** 将 GPIO2 与 GPIO0 连接。[2] 因 GPIO0 是 BOOT 管脚，GPIO2 是 TF DATA0 且两者直接相关，任何外接负载、上拉/下拉或外部模块的上电状态都可能影响烧录或启动。R43 在资料中未标为 NC，但仍建议通过实物电阻位和万用表导通测试确认实际贴装。

| 信号 | GPIO | 原理图上拉/连接 | 开发要求 |
|---|---:|---|---|
| DATA2 | GPIO12 | R41=10 kΩ/NC；R11=100 Ω 串联。[2] | 说明书称 GPIO12 为 Flash 电压选择相关脚；保持上电状态安全，避免外部强拉高。[1] |
| DATA3 | GPIO13 | R17=10 kΩ 上拉；R12=100 Ω 串联。[2] | 使用 TF 卡时不要复用。 |
| CMD | GPIO15 | R18=10 kΩ 上拉；R13=100 Ω 串联。[2] | 使用 TF 卡时不要复用。 |
| CLK | GPIO14 | R14=100 Ω 串联。[2] | 使用 TF 卡时不要复用。 |
| DATA0 | GPIO2 | R19=10 kΩ 上拉；R15=100 Ω 串联；R43=0 Ω 连到 GPIO0。[2] | 与使用说明“无上拉”冲突；以实物和实际下载测试为准。 |
| DATA1 | GPIO4 | R20=10 kΩ 上拉、R39=10 kΩ 上拉；R16=100 Ω 串联。[2] | 视为已带上拉的固定板载资源，不宜连接强下拉外设。 |

## 7. 传感器接口与电气注意事项

| 接口 | 连接器脚序与电路 | 使用说明中的功能 | 首测建议 |
|---|---|---|---|
| DHT11（JP1） | Pin 1=GND，Pin 2=DHS/GPIO25，Pin 3=+3.3 V；DHS 串 100 Ω，R44=10 kΩ 上拉。[2] | 单总线温湿度，说明书称一次通信约 4 ms。[1] | 读取温湿度并输出串口；不再额外并联低阻上拉。 |
| HC-SR04（JP2） | Pin 1=+3.3 V，Pin 2=Trig/GPIO32，Pin 3=Echo/GPIO33，Pin 4=GND；两条信号均串 100 Ω。[2] | 说明书列出 2–400 cm 与最高约 3 mm 测距精度。[1] | **原板接口按 3.3 V 供电**；换用其他模块时实测 Echo 高电平。 |
| WS2812（JP3） | Pin 1=RGB/GPIO26，Pin 2=VCC（+5 V 经 D1 1N4007），Pin 3=GND；RGB 串 100 Ω。[2] | 12 颗灯珠，单线控制任意颜色。[1] | 先移除 LCD 背光跳线，以低亮度单色验证。 |
| SR602/PIR（JP5） | Pin 1=RES/GPIO34，Pin 2=+3.3 V，Pin 3=GND；RES 串 100 Ω。[2] | 有人输出高、无人输出低；说明书称最大检测距离 5 m。[1] | GPIO34 只作输入，读取电平变化。 |
| NTC（JP4） | Pin 1=NTC，Pin 2=GND；R9=10 kΩ/1% 上拉到 +3.3 V，R8=100 Ω 串到 GPIO36/VP。[2] | 资料标为 10 kΩ NTC，25 ℃时约 10 kΩ，测温范围 −20 ℃ 至 125 ℃。[1] | 读取 ADC 原始值后，按实测参考电压、10 kΩ 分压和热敏参数换算温度。 |

## 8. 开发与排错清单

建议按“串口通讯 → 电源/LED → 用户按键 → 单一传感器 → LCD/触摸 → TF 卡”的顺序验证。第一版测试程序不应同时初始化 LCD、WS2812、TF 卡和多个传感器，以免 GPIO 复用、供电和启动问题互相掩盖。

| 现象 | 优先检查项 | 处理方向 |
|---|---|---|
| 无串口、无法下载 | Type-C 是否为数据线、CH340C 设备是否识别、LED1 是否点亮。 | 检查 USB 连接与 3.3 V 供电；更换数据线。 |
| 自动下载失败 | GPIO0/GPIO2、R43 相关外设和串口占用。 | 断开新增外设；按住 BOOT 后短按 RESET 再烧录。 |
| 烧录后不启动 | GPIO0、GPIO2、GPIO12 的复位时电平。 | 暂时断开 TF 卡/扩展，检查 R43 实物贴装与启动日志。 |
| LCD 黑屏 | 背光跳线、GPIO26 复用、SPI 初始化。 | 若刚测试过 WS2812，先恢复背光跳线。 |
| WS2812 与 LCD 同时异常 | GPIO26/背光资源冲突。 | 移除背光跳线后单独测试 WS2812。 |
| PIR 或按键异常 | GPIO34、GPIO35、GPIO39 是输入专用。 | 确认代码未将它们配置为输出。 |
| NTC 温度不准 | ADC 校准、10 kΩ 分压、传感器 B 值。 | 先记录 ADC 原始值；用已知温度点校准。 |
| TF 卡不稳定或下载失败 | GPIO2/GPIO0 的 R43 关联、上拉冲突、启动配置。 | 先验证不插卡下载；再读取实物电阻位和卡初始化时序。 |

## 9. 当前资料仍未给出的信息

下列事项无法由现有两份资料可靠得出，应在需要时通过实物、源码或器件数据手册补齐：

| 未给出事项 | 建议确认方式 |
|---|---|
| 实际模组 Flash 容量及分区上限 | 查看模组丝印，或在 ESP-IDF/Arduino 构建与运行日志中读取。 |
| R43、R19、R41 等电阻的实际贴装状态 | 查看 PCB 丝印和元件位，必要时断电测阻/导通。 |
| LCD 触摸控制器具体型号及初始化地址 | 查配套源码、FPC 丝印或读取 I²C 总线。 |
| AMS1117/USB 与扩展接口可持续供电电流 | 查实物器件标识和电源设计，不以原理图符号推断。 |
| 外部 WS2812、HC-SR04 等替代模块的电平兼容性 | 用万用表/示波器测量实际高电平与波形；需要时加电平转换。 |

## 参考资料

[1] 《ESP32 物联网开发套件使用说明》，用户提供 PDF，涵盖板载资源、配套传感器、下载方法与 TF 卡注意事项。  
[2] 《ESP32 开发板原理图 1.1》，用户提供 PDF，涵盖供电、自动下载、最小系统、传感器、SD 卡与 LCD 接口电气连接。

[1]: <https://files.manuscdn.com/user_upload_by_module/session_file/310519663889707187/WhQEUzMukJrxEkFX.pdf> "ESP32 物联网开发套件使用说明"
[2]: <https://files.manuscdn.com/user_upload_by_module/session_file/310519663889707187/xUDWDLsecVaXCxqz.pdf> "ESP32 开发板原理图 1.1"
